# Oasis Frontends Architecture Presentation

Using [reveal-md](https://github.com/webpro/reveal-md). Run it with docker from main directory:

```docker run --rm -p 1948:1948 -v $PWD:/slides webpronl/reveal-md:latest```
